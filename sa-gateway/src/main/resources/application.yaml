spring:
  application:
    name: api-gateway
  zipkin:
    baseUrl: http://zipkin:9411
server:
  port: 8082
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://eureka-server:${EUREKA_PORT:8761}/eureka